# 项目迭代面板（plan.md）

我们用这个文件做轻量迭代看板：每次开始一个新任务就新增一条卡片；实现完成并通过验收后，将卡片移动到 Done，并勾选验收标准。

## 需求卡片模板（复制后填写）

### [ID] 标题
- 用户故事：作为[角色]，我希望[能力]，以便[价值]
- 验收标准：
  - [ ] 条件 1
  - [ ] 条件 2
  - [ ] 条件 3
- 关联（可选）：页面/接口/关键文件
- 记录（可选）：创建日期 YYYY-MM-DD；完成日期 YYYY-MM-DD；备注

---

## Backlog（待办）

### [0012] 映射编辑器与导出（DDL/API草案）
- 用户故事：作为方案架构师/交付负责人，我希望在界面上编辑并导出“本体→数据库/接口”的映射结果，以便把模型转成可落地的工程资产包。
- 验收标准：
  - [ ] 提供映射视图/面板：可查看每个对象类型的表映射、字段映射、关系映射
  - [ ] 支持导出数据库资产：DDL（至少一种方言）+ 数据字典（字段口径/类型/必填）
  - [ ] 支持导出接口资产：接口清单 + 请求/响应 schema（可 JSON/GraphQL 二选一）
  - [ ] 映射质量校验：缺失映射、主键/外键、类型不兼容有明确提示
- 关联（可选）：components；lib/types；app/api
- 记录：创建日期 2026-03-30

---

### [0011] 规则与流程映射到接口契约
- 用户故事：作为研发负责人，我希望业务规则与业务流程能映射为可执行的接口契约与校验点，以便研发可以直接按接口与规则落地实现。
- 验收标准：
  - [ ] ActionType → 接口定义：endpoint/mutation 名称、输入参数、输出字段（与本体一致）
  - [ ] DataFlow → 编排建议：步骤与依赖关系能映射到接口调用链/工作流定义
  - [ ] BusinessRule → 校验与约束：规则可被标注为 DB 约束/应用校验/流程门禁三类（至少支持标注与导出）
  - [ ] 对 ERP 示例：CreatePR/CreatePO/ReceiveGoods 能导出对应接口草案与校验点
- 关联（可选）：lib/types/ontology-erp-sample.ts；components/semantic-query
- 记录：创建日期 2026-03-30

---

### [0010] 类与属性映射到数据库实体（表/字段/关系）
- 用户故事：作为数据架构师，我希望把本体中的类与属性映射到数据库实体（表/字段），并把关系映射到外键/关联表，以便形成可落地的数据模型设计。
- 验收标准：
  - [ ] ObjectType → Table：可配置表名、主键策略、索引建议
  - [ ] Property → Column：类型映射规则明确（STRING/DOUBLE/TIMESTAMP 等），必填/默认值可表达
  - [ ] LinkType → FK/Join Table：按基数（1-1/1-n/n-n）生成外键或中间表策略
  - [ ] 对 ERP 示例：purchase-requisition/material-erp/purchase-order/goods-receipt 的映射可生成一致的表结构草案
- 关联（可选）：lib/types；stores；导出模块
- 记录：创建日期 2026-03-30

---

### [0005] （待确认）下一迭代目标占位
- 用户故事：作为产品/研发协作方，我希望明确下一迭代的交付目标，以便可以拆分任务与验收
- 验收标准：
  - [ ] 明确本迭代的范围（做什么/不做什么）
  - [ ] 形成可验证的验收标准清单
  - [ ] 拆分为 1–3 条可交付卡片
- 记录：创建日期 2026-03-25

---

## In Progress（进行中）

### [0004] 重写本体图谱展示（Reagraph）
- 用户故事：作为用户，我希望在主画布以 Reagraph 方式展示本体图谱，以便更稳定地进行缩放拖拽、布局与节点联动查看。
- 验收标准：
  - [ ] 画布支持“图谱模式”展示（力导向布局，可缩放、拖拽）
  - [ ] 渲染不出现 SSR/水合阶段的 `window is not defined` 等异常
  - [ ] 节点至少展示对象与关系（ObjectType/LinkType），并能渲染边
  - [ ] 点击节点可联动右侧面板展示对应详情
- 关联（可选）：components/graph-canvas/ontology-canvas.tsx；components/graph-canvas/ontology-knowledge-graph.tsx；package.json
- 记录：创建日期 2026-03-25

---

## Done（已完成）

### [0009] 语义查询输出字段随业务模块正确切换
- 用户故事：作为采购业务用户，我希望语义查询的“将生成的数据或者生成的实例”能随当前动作/模块展示正确输出字段，以便验证解析结果是否符合当前业务场景。
- 验收标准：
  - [x] 在“ERP采购业务模块”中提问“创建采购申请，物料 A001 数量 100”，输出字段展示 PR 编号等采购申请相关内容
  - [x] 在“图书馆模块”中提问借阅相关问题，输出字段展示借阅 ID/应还日期/借阅状态等借阅相关内容
  - [x] 当 LLM 未返回 output 时，界面仍可根据动作定义推导正确输出字段
- 关联（可选）：components/semantic-query/semantic-query-input.tsx
- 记录：创建日期 2026-03-30；完成日期 2026-03-30

---

### [0008] 提交当前版本并合并到 main（master）
- 用户故事：作为协作方，我希望将当前版本提交到 Git 并可合并到主分支，以便稳定发布与协作开发。
- 验收标准：
  - [x] 工作区变更已提交到 feature 分支并推送远端
  - [x] 与 origin/main 合并无冲突（或冲突已解决）
  - [x] main 分支已更新并推送远端
  - [x] `npm run lint` 与 `npm run build` 通过
- 记录：创建日期 2026-03-28；完成日期 2026-03-28

### [0007] 引入新的示例场景：ERP采购业务模块
- 用户故事：作为业务建模用户，我希望在加载示例中能选择“ERP采购业务模块”，以便除了图书馆外，有更典型的企业级业务场景供我参考与推演。
- 验收标准：
  - [x] 定义 ERP 采购业务的本体数据（包含实体、关系、操作、数据流、规则、AI模型等）
  - [x] 系统支持多个示例场景的切换/加载（如图书馆、采购业务）
  - [x] 顶部导航或某处的加载示例功能支持选择
  - [x] 业务场景沙盘与语义查询等功能可以无缝兼容新的示例
- 记录：创建日期 2026-03-27

### [0006] 业务场景沙盘：MECE 穷举本体可执行场景
- 用户故事：作为本体业务模型设计器，我希望一键生成“业务场景沙盘”，穷举当前本体可推演的业务场景清单，以便度量模型对业务场景的覆盖度与盲区。
- 验收标准：
  - [x] 顶部栏新增「业务场景沙盘」按钮，可打开场景面板/抽屉并触发生成。
  - [x] 系统从当前本体数据生成结构化输入（对象/关系/动作/规则等摘要）并调用大模型推演。
  - [x] 输出符合 MECE：按“麦肯锡金字塔”从主题→分组→子场景→可执行用例逐层展开，分组不重叠且尽量穷尽。
  - [x] 场景输出包含：场景名称、目标、参与角色/对象、触发条件、关键动作链、前置/后置条件、可观测结果与覆盖度标记。
  - [x] 支持导出/复制场景结果（Markdown/JSON 任选其一即可），便于评审与度量。
- 记录：创建日期 2026-03-26；完成日期 2026-03-27

### [0003] 为资源预测增加交互与骨架屏加载效果
- 用户故事：作为业务用户，我希望在推演业务资源和数据结构时，看到明确的“加载中”和“骨架屏”反馈，并在出错时提供“重新推演”的入口，以免我不清楚后台进度并能够自行恢复。
- 验收标准：
  - [x] 在预测 Agent 运行期间，右侧面板提供有科技感的骨架屏动画与文案。
  - [x] 在预测报错时，提供明显的错误提示和「重新推演」按钮。
  - [x] 点击重新推演能正确重新触发 API 请求并更新结果。
- 记录：创建日期 2026-03-25；完成日期 2026-03-25

### [0002] 建立 plan.md 轻量迭代面板
- 用户故事：作为项目协作方，我希望用一个简单的文件记录已完成与计划需求，以便对齐沟通与验收
- 验收标准：
  - [x] 仓库根目录存在 plan.md
  - [x] plan.md 包含 Backlog / In Progress / Done 三个区域
  - [x] 每条卡片包含“用户故事 + 验收标准”
  - [x] 提供可复制的卡片模板
- 记录：创建日期 2026-03-25；完成日期 2026-03-25

### [0001] 界面区域模块划分与命名约定
- 用户故事：作为产品与研发协作方，我希望把界面区域按模块统一命名，以便后续需求描述有共同语境
- 验收标准：
  - [x] 给出清晰的界面模块划分（至少包含左侧导航、中心画布、右侧面板、底部预览、顶部栏）
  - [x] 明确“本体配置解析区”与“本体语句解析区”的位置与职责
  - [x] 命名可用于后续 PRD/任务卡沟通（不依赖截图解释）
- 记录：创建日期 2026-03-25；完成日期 2026-03-25
